<form method="post" action="{{ path('pourcentage_e_c_module_delete', {'id': pourcentage_e_c_module.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pourcentage_e_c_module.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
