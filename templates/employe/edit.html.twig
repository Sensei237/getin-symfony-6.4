{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}{{"GESTION DU PERSONNEL "}}{% endblock %}

{% block pageTitle %} {{ "GESTION DU PERSONNEL" }} {% endblock %}

{% block contentTitle %} {{ editMode ? "Modifier une filière" : "Créer une filière" }} {% endblock %}

{% block footerContent %}
    
{% endblock %}

{% block fil_ariane %}
    <li class="breadcrumb-item"><a href="{{ path('employe') }}">personnel</a></li>
    <li class="breadcrumb-item active">{{ editMode ? "modifier" : "créer" }}</li>
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-7">
            <div class="list-group-item">
                {{ form_start(form) }}
                    {{ form_row(form) }}
                    <button class="btn btn-primary" type="submit">{{ btn_title }}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}


